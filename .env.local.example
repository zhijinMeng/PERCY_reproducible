# 复制为 .env.local（已在 .gitignore，不会提交）：
#   cp .env.local.example .env.local
# 在 .env.local 里填入真实值；./ros1_ari.sh 会自动读入并传入容器。

OPENAI_API_KEY=
ARI_ROS_IP=10.68.0.130
ARI_ROS_USE_LOOPBACK=0
PERCY_DATA_DIR=/workspace/percy_data
