# 机器人网络与 SSH（实验室当前用法）

## 网络

- **WiFi**：连接机器人侧热点（例如 SSID **`ari-27c`**），用于 `10.68.0.0/24` 网段上的 ROS/SSH。
- **有线**：接实验室/校园网，用于互联网；系统应优先把默认路由走有线（metric 低于 WiFi）。

## SSH（已验证可用）

- **用户**：`pal`
- **主机名**：`ari-27c`（解析到 **`10.68.0.1`**）
- **命令示例**：
  - `ssh pal@ari-27c`
  - 或配置好 `~/.ssh/config` 后：`ssh ari-27c`（见下）

机器人侧为 **Ubuntu 20.04**（示例提示里为 LTS）；登录后若需公网 `apt`，依赖本机有线出口。

## `~/.ssh/config` 建议条目

使用 `Host ari-27c`，`HostName 10.68.0.1`，`User pal`。具体以本机 `~/.ssh/config` 为准。

## 说明

- 笔记本在机器人 WiFi 上的地址由 **DHCP** 分配（例如曾为 `10.68.0.129`），每次连接末位可能变化。
- **WiFi 密码 ≠ SSH 密码**；SSH 使用账户 `pal` 的口令（或后续配置的公钥）。
